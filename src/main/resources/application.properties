spring.application.name=hotel-admin-system
# === Configuración de conexión MySQL ===
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_db?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=414262
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === Configuración de JPA (sin modificar la BD) ===
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# === Opcional: formato legible de logs SQL ===
spring.jpa.properties.hibernate.format_sql=true
